---
- name: Deploy local app from Containerfile
  hosts: localhost
  connection: local
  vars:
    domain_name: "{{ ansible_fqdn }}"
  roles:
    - role: containerfile_app
      vars:
        containerfile_app_directory: "{{ app_dir | mandatory }}"